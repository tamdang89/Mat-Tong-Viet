Place your banner image here and name it `banner-1920x600.jpg`.

Requirements:
- File: assets/banner-1920x600.jpg
- Size: 1920 x 600 pixels (recommended)
- Format: JPG or PNG

After adding the image, reload `index.html` and the hero banner will display the new image. If you want a different filename, update the `background-image` URLs in `index.html` accordingly.